class Params {
  Params({this.min, this.max, this.numOfStars,this.globalMin, this.globalMax, this.stepSize});

  double globalMin;
  double min;
  double max;
  double globalMax;
  double stepSize;
  int numOfStars;
}
